Django>=4.0,<5.0
python-dotenv>=0.19.0
# Add other Python dependencies here if needed, e.g., for Genkit Python SDK
# Pillow # For image handling if models with ImageField are used
# psycopg2-binary # If using PostgreSQL
